{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Edit Page{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('editor') }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('editor') }}
{% endblock %}

{% block body %}
    <div class="btn-group float-end">

        <a class="btn btn-outline-secondary" href="{{ path('page_index') }}">back to list</a>

        {{ include('page/_delete_form.html.twig') }}
    </div>

    <h1>Edit Page</h1>

    {{ include('page/_form.html.twig', {'button_label': 'Update'}) }}


{% endblock %}
